# Домен, на котором будет сайт (без https://)
DOMAIN=wishlistpizda.duckdns.org

# Секретный ключ для JWT (можно сгенерировать: openssl rand -hex 32)
SECRET_KEY=change-me-to-a-random-secret-key

# Токен Telegram бота (от @BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Часы между проверками цен (24 = раз в сутки)
PARSER_INTERVAL_HOURS=24
